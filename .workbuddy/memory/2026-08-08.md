# 2026-08-08 工作日志

## 全量扫描项目并生成详细介绍
- 扫描了 FINEX（AI 智能股票分析系统）项目真实结构，对照 README 核实。
- 产出：`docs/项目详细介绍.md`（182 行，约 14KB）。
- 关键事实：
  - 架构 = Frontend(index.html 3391行) → FastAPI(backend/server.py) → LangGraph 工作流(agents/src) → MCP Server(mcp-server，29 个 A股工具，底层 Baostock)。
  - 工作流拓扑：start_node → 4 个并行分析 Agent(fundamental/technical/value/news) → summarizer(汇总+报告+PDF) → END；main.py 与 server.py 共用 build_workflow() 工厂。
  - MCP 工具实测 29 个（README 写 20+）；scripts/ 下约 1700 个 .py/.pyc 是嵌套 venv 缓存，非业务代码；AGENTS.md 仅 1 行占位。
- 后续路线图(TODO)：引入 LiteLLM 网关层，支持外部模型宕机时降级到本地 Ollama。
